'''par_dict={}
        for item in main_list:
            out_arr=[]
            for i in item:
                if i in inp_arry:
                    out_arr.append(i)
            key=''
            if item==commamds:
                key='commands'
            elif item==days:
                key='days'
            elif item==qust:
                key='quest'
            elif item==info:
                key='info'
            elif item==connectives:
                key='connectives'
            par_dict[key]= out_arr
        self.par_dict = par_dict'''
        